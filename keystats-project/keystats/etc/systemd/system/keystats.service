[Unit]
Description=Keystats Keyboard Activity Monitor
After=local-fs.target

[Service]
Type=simple
ExecStart=/usr/bin/keystats-daemon --foreground
ExecStop=/bin/kill -TERM $MAINPID
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
